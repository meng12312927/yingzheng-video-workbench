# ============================================
# 映证｜活动视频需求与审核工作台 — 依赖清单
# 安装命令：pip install -r requirements.txt
# ============================================

# LLM API（大模型调用）
openai>=1.0.0

# 语音识别（Whisper GPU 加速版）
faster-whisper>=1.0.0

# 视频处理
ffmpeg-python>=0.2.0
moviepy>=2.0.0

# 数据模型校验
pydantic>=2.0.0

# 混合证据检索：本地数值计算与 FAISS 向量索引
numpy>=1.24.0
faiss-cpu>=1.8.0

# 环境变量管理
python-dotenv>=1.0.0

# Web 界面
gradio>=4.0.0
# Gradio 4.x 仍依赖 huggingface_hub.HfFolder；1.x 已移除该接口。
huggingface-hub<1.0

# 进度条
tqdm>=4.0.0

# 测试
pytest>=8.0.0
